import AuthenticationComponent from "../../components/authentication/authentication.component.jsx";

const Authentication = () => {
  return (
    <div>
      <AuthenticationComponent />
    </div>
  );
};

export default Authentication;
